---
// Terminal-style code panel with traffic-light header dots.
// Inspired by reference design — for surfacing real schema / sample data.
interface Props {
  caption: string;
}
const { caption } = Astro.props;
---

<div class="overflow-hidden rounded-lg border border-white/10 bg-[#0a1428]">
  <div class="flex items-center justify-between border-b border-white/10 bg-white/5 px-4 py-2.5">
    <div class="flex gap-1.5" aria-hidden="true">
      <span class="h-2.5 w-2.5 rounded-full bg-red-400/80"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-amber-400/80"></span>
      <span class="h-2.5 w-2.5 rounded-full bg-emerald-400/80"></span>
    </div>
    <span class="font-mono text-xs text-white/60">{caption}</span>
  </div>
  <div class="overflow-x-auto p-5 font-mono text-[13px] leading-7 text-white/90">
    <slot />
  </div>
</div>
