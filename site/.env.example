PUBLIC_GA_MEASUREMENT_ID=

# Full URL of the Cloudflare Worker after deploy (e.g. https://horsorion-lead.your-subdomain.workers.dev).
# Same value as GitHub Actions secret PUBLIC_LEAD_ENDPOINT so production forms POST to the Worker.
PUBLIC_LEAD_ENDPOINT=
